<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.accountbook.project.mapper.IncomeMapper">
    <select id="selectIncome" parameterType="java.lang.Object" resultType="Income">
        select * from ac_income where id = #{id}
    </select>
</mapper>